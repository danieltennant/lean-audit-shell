// Phase 5 — ZshBackend using portable-pty (macOS v1, not yet implemented)
//
// portable-pty works correctly on macOS; the missing ConPTY flags
// (PSEUDOCONSOLE_WIN32_INPUT_MODE etc.) are Windows-only concerns.
